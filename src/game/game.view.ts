class GameView {
  // logica menu playmatch
  // public playMatch(home: Team, away: Team, round: Round) {
    // home.score.totalScore += 50;
    // const winner = home;

    // round.winners.push(winner);
    // return home;
  // }
}
